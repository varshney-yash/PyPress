{
    "builds": [{
        "src": "PyPress/wsgi.py",
        "use": "@ardnt/vercel-python-wsgi",
        "config": { "maxLambdaSize": "15mb" }
    }],
    "routes": [
        {
            "src": "/(.*)",
            "dest": "PyPress/wsgi.py"
        }
    ]
}
