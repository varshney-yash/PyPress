{
    "builds": [{
        "src": "PyPress/wsgi.py",
        "use": "@ardnt/vercel-python-wsgi",
        "config": { "maxLambdaSize": "15mb", "runtime": "python3.8" }
    }],
    "routes": [
        {
            "src": "/(.*)",
            "dest": "PyPress/wsgi.py"
        }
    ]
}
